

<main>

  <div class="">
    <% sneakers.forEach(sneaker => {%>
      <div>
      <a href="/sneakers/<%= sneaker.id %>">

      <img width="500px" height="500px" src="<%= sneaker.image %>" />
      <h3><%= sneaker.model %></h3>
      <p>£<%= sneaker.price %></p>
      </a>
      </div>
      <% }) %>
    </div>
</main>
